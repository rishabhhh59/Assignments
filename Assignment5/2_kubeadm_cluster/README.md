# Step 2: Create Kubernetes Cluster Using kubeadm

This script sets up a Kubernetes cluster on an Ubuntu system using kubeadm.

## Notes:
- Should be run on a clean Ubuntu VM
- Requires root/sudo access
